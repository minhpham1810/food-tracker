# Point this at the FastAPI backend, reachable from your phone on the same Wi-Fi network.
# "localhost" does NOT work here -- from a physical phone that means the phone itself.
# Find this Mac's LAN IP with: ipconfig getifaddr en0
EXPO_PUBLIC_API_BASE_URL=http://<YOUR_LAN_IP>:8010/api
